"""
fetch the texts for docno's in trec ranking file
    for now there is no fields
input:
    trec ranking
    doc_text directory
        each file is a partition parsed text
            docno \t url \t text
output:
    doc info file
        docno \t h{'docno:', 'field(now it is bodyText':text}

"""



